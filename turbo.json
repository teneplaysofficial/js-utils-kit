{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "sync": {},
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$"],
      "env": ["NODE_ENV"],
      "outputs": ["dist/**"]
    },
    "test": {
      "inputs": ["$TURBO_DEFAULT$"],
      "env": ["NODE_ENV"],
      "outputs": ["coverage/**"],
      "cache": true
    }
  }
}
