---
'@js-utils-kit/pm': minor
---

Add `detectPM()` utility to detect the package manager used in a project (npm, pnpm, yarn, or bun) based on environment variables, lockfiles, or the `packageManager` field in `package.json`.
